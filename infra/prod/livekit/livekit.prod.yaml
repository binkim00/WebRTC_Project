port: 7880

log_level: info

rtc:
  tcp_port: 7881
  udp_port: 7882
  use_external_ip: true

redis:
  address: redis:6379
  db: 1

# 운영에서는 secret을 Git에 남기지 않기 위해 이 파일을 직접 마운트하지 않는다.
# 실제 설정은 docker-compose.prod.yml의 LIVEKIT_CONFIG에서 구성된다.
