services:
  livekit:
    image: livekit/livekit-server:v1.13.4
    container_name: melly-livekit
    restart: unless-stopped

    command:
      - --config
      - /etc/livekit.yaml

    ports:
      - "7880:7880"
      - "7881:7881"
      - "50000-50100:50000-50100/udp"

    volumes:
      - ./livekit.yaml:/etc/livekit.yaml:ro